# OptimizeUpdatesFilter will remove all columns from a row-based replication UPDATE
# where the old value and the new value are the same
replicator.filter.optimizeupdates=com.continuent.tungsten.replicator.filter.OptimizeUpdatesFilter
