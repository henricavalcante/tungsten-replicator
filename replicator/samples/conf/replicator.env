# Replicator environmental variable configuration file. 
#
# This file sets environmental variables used by the replicator script.  
# Values are auto-generated by tpm.  You can uncomment values to 
# parameterize replicator startup.  Where commented out sample values are
# provided. 

# Location of Java distribution. 
#JAVA_HOME=/usr/lib/jvm/java-7-openjdk-amd64

# RMI options, defaults to allowing remote access
REPLICATOR_RMI_OPTS="-Dcom.sun.management.jmxremote -Dcluster.home=../../cluster-home -Djava.rmi.server.hostname=@{HOST.HOST}"

# Replicator heap size, defaults to 1024M.
REPLICATOR_HEAP_SIZE=-Xmx@{REPL_JAVA_MEM_SIZE}m

# Replicator GC policy, defaults to concurrent GC. 
#REPLICATOR_GC_POLICY="-XX:+UseConcMarkSweepGC -XX:+CMSIncrementalMode"

# You may need to set the Java platform charset to replicate heterogeneously
# from MySQL using row replication.  This should match the default charset
# of your MySQL tables.  Common values are UTF8 and ISO_8859_1.  Many Linux
# platforms default to ISO_8859_1 (latin1).
@{#(REPL_JAVA_FILE_ENCODING)}REPLICATOR_FILE_ENCODING=-Dfile.encoding=@{REPL_JAVA_FILE_ENCODING|UTF8}

# !!! WARNING !!!
# Replicators by default set the JVM time zone to GMT.  If you need
# to set the JVM time zone alter it in services.properties, *not* here.
# The wrapper.conf time zone option sets the host time zone and is retained
# in case of compatibility problems with older replicator logs.  Do not enable
# this option unless you know replicator internals or are instructed to use it
# by support.
@{#(REPL_JAVA_USER_TIMEZONE)}REPLICATOR_USER_TIMEZONE=-Duser.timezone=@{REPL_JAVA_USER_TIMEZONE|GMT}

# Port for JVM remote debugging
#REPLICATOR_DEBUG_PORT=54002

# JVM options for debugging. 
#REPLICATOR_JVMDEBUG_OPTS="-enableassertions -Xdebug -Xnoagent -Djava.compiler=none -Xrunjdwp:transport=dt_socket,server=y,suspend=n,address=$REPLICATOR_JVMDEBUG_PORT"

# Miscellaneous Java VM options not included above.  Samples shown below. 

# Options to perform remote profiling with Yourkit.
#JVM_OPTIONS="-agentpath:/opt/yourkit/yjp-9.5.4/bin/linux-x86-64/libyjpagent.so=port=10001"

# Option to enable debugging of SSL.
#JVM_OPTIONS=-Djavax.net.debug=ssl

# Replicator log directory, defaults to $RPHOME/log
#REPLICATOR_LOG_DIR=/opt/continuent/tungsten/tungsten-replicator/log

# File to contain running PID.
#REPLICATOR_PID=/opt/continuent/tungsten/tungsten-replicator/var/replicator.pid
