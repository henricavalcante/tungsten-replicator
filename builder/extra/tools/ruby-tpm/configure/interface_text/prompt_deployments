You must define the hostname, IP address and home directory for each 
installation of the Tungsten Replicator.  If you have multiple installations 
on a single host, the home directory must be unique.